services:
  - type: web
    name: paraphraser-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python paraphraser_api.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
