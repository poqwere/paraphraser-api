services:
  - type: web
    name: paraphraser-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "startCommand: gunicorn -b 0.0.0.0:$PORT paraphraser_api:app"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
